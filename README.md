# Spring Boot API - Render (Docker)

## Despliegue en Render
1. Crear un nuevo **Web Service**
2. Elegir **Docker**
3. Conectar el repositorio
4. Render detectará automáticamente el Dockerfile
5. Agregar variables del archivo `.env` en el panel de Environment

Endpoint:
GET /